# Text-file inputs

Attach plain-text files (source code, logs, markdown) as a `ContentPart` using the same `File` type as PDFs.

See [`examples/text-file-inputs/main.go`](../../examples/text-file-inputs/main.go), including the `TextBuilder` helper and multi-file examples.
